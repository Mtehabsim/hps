defaults:
    - zc-gcg-prompt

# Apple-to-apple Euclidean CONTROL for z-jailbreak-gcg-rpf: identical mean-pool-then-LR
# pipeline on the full 4096-dim activation, but WITHOUT the x0 hyperbolic lift.
# rpf vs rpfnox0 under GCG isolates the effect of the single Lorentz radial coordinate
# (architecture held fixed) -> the decisive "does hyperbolic help" test under hard-token.
metric: "HPSMetric"
tag: "gcg-rpfnox0"

concept_data_module: "HIDHarmfulConceptDataModule"
example_idx: 0

metric_config:
    layers: [1, 7, 13, 19, 25, 32]
    kappa: 0.1
    proj_dim: 64
    hps_epochs: 50
    feature_set: "raw_proj_full_nox0"
