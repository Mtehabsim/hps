# zero-try (kept files) — abandoned "physics + Mahalanobis" approach

Representative files from the earliest iteration: detecting GCG jailbreaks via hand-engineered
trajectory "physics" (Menger curvature, CLDP, velocity, attention entropy) + Mahalanobis anomaly
scoring on Vicuna/Llama. Abandoned (FPR walls) for the learned-geometry approach in `src/`.
Kept here: `shadow3.py` (canonical 5-pillar experiment), `shadow2.py` (most-evolved detector +
rationale), `01_ground_truth_labeler.py` (refusal-prefix data oracle), `step4_massive_fpr_telemetry.py`
(mass benign-FPR harness). The rest of the original folder (≈34 scratch/duplicate files) was deleted.
See `../README.md` for the full lineage.
