# first-try (kept file) — early 4-test validation suite

Kept: `test1_gromov_delta.py` — Gromov δ-hyperbolicity of the residual stream ("is the space
intrinsically hyperbolic?"), the one analysis with no counterpart in `src/`. The rest of the first
iteration (config/dataset/utils/run_all + test2–4, all superseded by `src/`) was deleted.
See `../README.md`.
