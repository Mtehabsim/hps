# second-try (kept file) — original design doc

Kept: `HPS_Document.md` — the original Llama-3-8B HPS design document (early framing and the
pre-validation, optimistic ~1.0 AUROC results). The rest of the second iteration (a duplicated 4–5
test suite + step2/ pipeline, all superseded by `src/hps_core.py` etc.) was deleted. See `../README.md`.
