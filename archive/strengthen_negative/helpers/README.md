# helpers — support module for strengthen_negative

Shared helpers imported by the `strengthen_negative/` experiment scripts (see `../README.md`):
`lorentz_ops.py` (Lorentz/hyperbolic geometry operations) and `__init__.py`. Not used by `src/`;
kept so the archived `strengthen_negative` scripts remain self-contained.
