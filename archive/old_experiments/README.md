# old_experiments — numbered experiment scripts (early arcs)

The `experiment6–13` / `test1–5` / `experiment_mitigation*` lineage and their `run_all.py`. These are
the chronological experiment scripts from earlier arcs; their surviving logic was consolidated into
`src/` (`hps_core.py`, `statistical_tests.py`, `hierarchical_detector.py`, etc.). Kept for provenance.
