# viz_old — old visualization & figure-generation scripts

Plotting/figure/slide generators from earlier arcs. The current kept figure script is
`src/plot_obfuscation.py`; figures themselves live in `figures/`.

Includes: paper/meeting figure generators (`generate_paper_plots.py`, `generate_meeting_charts.py`,
`generate_methodology_diagram.py`, `fig_*.py`, `paper_supplementary.py`, `build_slides.py`) and
activation/Poincaré visualizers (`visualize_*.py`). Reference only.
